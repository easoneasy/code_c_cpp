#include <iostream>
#include <vector>
#include <ostream>
#include <iterator>
#include <algorithm>
#include <numeric>
#include <fstream>
#include <algorithm>
#include <list>
using namespace std;


/*
    迭代器适配器
        1、back_insert_iterator  尾部插入的迭代器
        2、front_iterator   头部插入的迭代器
        3、insert_iterator  中间位置插入的迭代器
        都是类型
        
        back_inserter()  创建back_insert_iterator
        front_inserter()  创建front_iterator
        inserter()        创建insert_iterator
        是函数，返回对应的迭代器，直接调用函数，创建迭代器
*/

void test1()
{
        
}

int main(int argc,char *argv[])
{

    test1();

    return 0;
}

